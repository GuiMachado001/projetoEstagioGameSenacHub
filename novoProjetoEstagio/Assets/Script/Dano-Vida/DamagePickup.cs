using UnityEngine;

public class DamagePickup : MonoBehaviour
{
    // Esse script pode ser deixado vazio, pois a lógica de diminuição de vida já está no PlayerController.
    // O objetivo desse script é simplesmente identificar que o objeto é um "DamagePickup" e ser destruído ao ser tocado.
}
